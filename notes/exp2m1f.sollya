pretty = proc(u) {
  return ~(floor(u*1000)/1000);
};

d = [-2^-5, 2^-5];
f_exp2m1f = (2^x - 1)/x;
Q = fpminimax(f_exp2m1f, 5, [|D...|], d);

err_p = -log2(dirtyinfnorm(Q-f_exp2m1f, d));
display = decimal;
print (Q);
display = decimal;
print ("absolute error:",pretty(err_p));

for i from 0 to degree(Q) by 1 do printdouble(coeff(Q, i));