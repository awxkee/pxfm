pretty = proc(u) {
  return ~(floor(u*1000)/1000);
};

d = [-2^-8, 2^-7];
f_log2p1 = log2(1 + x)/x;
Q = fpminimax(f_log2p1, 6, [|D...|], d);

err_p = -log2(dirtyinfnorm(Q-f_log2p1, d));
display = decimal;
print (Q);
display = decimal;
print ("absolute error:", pretty(err_p));

for i from 0 to degree(Q) by 1 do printdouble(coeff(Q, i));